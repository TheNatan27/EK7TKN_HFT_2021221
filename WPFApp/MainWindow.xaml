<Window x:Class="WPFApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"></ColumnDefinition>
            <ColumnDefinition Width="240"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="60"></RowDefinition>
            <RowDefinition Height="370"></RowDefinition>
        </Grid.RowDefinitions>
        <Button Content="Button" Grid.ColumnSpan="1" HorizontalAlignment="Center"  VerticalAlignment="Center" Click="btn_show_loginwindow"/>
        <StackPanel Grid.Column="1" Grid.Row="1">
            <Label Content="Name"></Label>
            <Label Content="{Binding name_bt}"></Label>
            <Label Content="Email"></Label>
            <Label Content="{Binding name_bt}"></Label>
            <Label Content="Age"></Label>
            <Label Content="{Binding name_bt}"></Label>
            <Label Content="Weight"></Label>
            <Label Content="{Binding name_bt}"></Label>
            <Label Content="Height"></Label>
            <Label Content="{Binding name_bt}"></Label>
            <Label Content="User ID"></Label>
            <Label Content="{Binding name_bt}"></Label>
            <Label Content="Premium"></Label>
            <CheckBox x:Name="check_premium"></CheckBox>
        </StackPanel>

    </Grid>
</Window>
